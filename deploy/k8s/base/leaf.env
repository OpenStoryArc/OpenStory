# The hub the sidecar federates to, as nats://<token>@<hub-tailscale-host>:7422.
# The placeholder keeps the leaf block valid and federates nowhere: the
# sidecar retries its own loopback and the node stays solo until this is set.
NATS_LEAF_URL=nats://127.0.0.1:7422
