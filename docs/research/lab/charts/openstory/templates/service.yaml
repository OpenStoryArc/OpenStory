apiVersion: v1
kind: Service
metadata:
  name: {{ include "openstory.fullname" . }}
  labels:
    {{- include "openstory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openstory.selectorLabels" . | nindent 4 }}
