# The boot pass, bounded

**Date:** 2026-09-25 · **Status:** plan, ready to work · **Branch:** `feat/reel-chart-beats-kindle` (or a branch stacked on it) · **Owner gate:** nothing rolls to a node until B-00 is green.

## Why now

The node loads every session's full event list at boot, one session at a time, to read two small fields: the parent session of a subagent (`data.session_id` on any of its events) and the working directory (the first event that carries a `cwd`). On a fleet store of 3050 sessions that pass alone drives anonymous memory to about 3.4 GB before the async replay starts; replay and the live consumer actors add the rest. Tonight the hub died at 4 GB, then at 5 GB, and the owner's mini died at 2 GB and again at 3 GB on the same image. Every earlier round bounded what the node keeps or raised what the box gives, and none of them changed how the node reads.

The record (via `agent_search` on the local node, 2026-09-25):

| when | where | what was said | what was done |
|---|---|---|---|
| 2026-04 | co-founder's workspace | "boot replay loading 241 sessions / 44669 events into memory at once" · backlog entry *Streaming Boot Replay (bounded-memory)*: "the server loads its entire event history into memory at boot" | entry never reached master; async replay landed instead |
| 2026-04-16 | branch triage | "cache could balloon mid-replay. There's no eviction… if that ever exceeds RAM, OOM" | noted |
| 2026-07-08 | openstory-deploy `4c7381a` | node "sat pinned at 99% of 2G" | node 2G → 4G, NATS 512M → 4G |
| 2026-07-10..13 | `cecba5c` … `12fecae` | bounded read-through projection: ProjectionCache, PayloadCache, pins, gauges | bounds what is kept after boot, not what is loaded during boot |
| 2026-07-12 | owner session | "it's back at 93% memory, the recurrence risk is real" | memory file updated |
| 2026-09-25 | this loop | cache bound 1.5G, limit 5G, PR for 8G, resize discussed | the boot pass read, this plan |

## Facts the plan stands on

- `boot_from_sqlite` (`rs/server/src/state.rs`) calls `event_store.session_events(&row.id)` for every session row and feeds the whole list to `detect_subagent_relationship` (reads `data.session_id`) and `extract_cwd_from_events` (`find_map` over events for a `cwd`). Both answers come from the first few events in practice.
- `replay_boot_sessions` (`rs/server/src/ingest.rs`) walks every session again with full events to fill projections and session rows.
- The sessions table already carries `project_id`, `project_name`, `label`, `custom_label`, `branch`, `event_count`, `first_event`, `last_event`, `host`, `user`, `origin_agent`; columns are added with idempotent `ALTER TABLE` (`rs/store/src/sqlite_store.rs`). Mongo mirrors the row in `upsert_session` (`rs/store/src/mongo_store.rs`) and already extracts `parent_uuid` per event.
- The boot phase lives in `rs/server/src/boot.rs` (`Phase`, `set_serving()`); `/api/health` is 503 with a body until serving.
- Consumer actors are supervised and start at boot; on the hub they drained a 13k-message backlog with 1000 events in flight each while replay ran.
- cgroup `memory.stat` on the hub during replay: anon 3.98 GB, file 1.0 GB at 703 of 3050 sessions. The bytes are heap, not page cache, and not inside the bounded caches.
- The mini's store: 2.97 GB SQLite, 1329 JSONL files, 1259 sessions. Old image steady state 1.36 GB; this image reaches serving at 2.99 GB and is killed.

## Rows

Same protocol as `REQUIREMENTS.md`: red spec committed alone, then the code; the gate runs `just test`; the loop log takes its times from `date`.

| id | requirement | red spec |
|---|---|---|
| B-00 | **A boot-memory harness.** `scripts/boot_memory.py` builds a synthetic store (N sessions; a tail of large sessions with 8k events and tool outputs of tens of KB, sized so the whole store is ~3 GB of JSONL, shaped like the fleet's agent sessions), boots `open-story serve` against it on a free port, samples RSS every second until `/api/health` says serving, and prints peak RSS per phase as health can see them (no-listen = reconcile + boot pass + working-set reproject, then starting, replaying, serving, and a settle sample). `--test` covers the parser and the phase split. Gate for the branch: **1259-session fixture, peak under 2 GB**. Real-data validation on the mini's store copy is an owner-run step, not CI. | script `--test`; a `just boot-memory` recipe; the number in the loop log |
| B-01 | **Ask the store for the two facts.** `EventStore::session_boot_facts(session_id) -> BootFacts { parent_session: Option<String>, cwd: Option<String> }`. SQLite: one query with `json_extract` over the first 32 events by time. Mongo: same with a projection and a limit. No event bodies cross the trait. | conformance helper on both backends: a session whose parent link and cwd sit in events 1 and 3 answers correctly; a session with neither answers `None`; a 5000-event session answers in under 5 ms |
| B-02 | **The boot pass uses B-01 and nothing else.** `boot_from_sqlite` never calls `session_events`. | a recording `EventStore` wrapper asserts zero `session_events` calls during `AppState` boot; the existing subagent-parent and project-resolution tests stay green |
| B-03 | **Stop recomputing.** Sessions rows gain `parent_session` and `cwd`, filled by the persist consumer from the first batch that carries them; the boot pass reads rows only; `open-story reconcile` backfills old rows once via B-01. Idempotent `ALTER TABLE` on SQLite; field on the Mongo document. | conformance: upsert then read back on both backends; reconcile on a store with empty columns fills them; boot with filled columns makes zero B-01 calls |
| B-04 | **One walk, not two.** `replay_boot_sessions` fills any missing boot facts as it walks, so a store with empty columns costs one read, not two. | test: a store with no columns filled boots with exactly one `session_events` per session |
| B-05 | **Consumers start at serving.** Config `consumers_start = "serving"` (default) or `"boot"`. The supervisors hold until `set_serving()`; health shows `consumers.<name>.state = pending_start` until then; the verdict does not raise `consumer_dead` for a pending actor. | test: with the default, no consumer subscribes before the phase flips; with `"boot"`, behavior is today's; health body and verdict cover `pending_start` |
| B-06 | **Give memory back.** `tikv-jemallocator` behind a default-on feature in `open-story-cli`, `background_thread:true,dirty_decay_ms:1000,muzzy_decay_ms:1000`; the plain-glibc build stays available. | B-00 peak and settle-after-serving numbers with and without the feature, in the loop log; the settle number must fall |
| B-07 | **Say it before the kernel does.** Health gains `process.memory_limit_bytes` (cgroup v2 `memory.max`, else null) and the verdict raises `memory_pressure` (warn at 75 %, critical at 90 % of the limit). The probe script and the dashboard's verdict match. | pure `verdict` test on a body with rss 4.6 G and limit 5 G → critical `memory_pressure`; probe script test; UI `verdictFor` test |
| B-09 | **The read model is bounded during replay.** (a) `SessionProjection::heap_bytes` counts what it holds — `seen_ids`, `depths`, `parents`, `filter_counts`, `file_touches`, `full_payloads`, the summary fold, and the `ViewRecord` structs with every string they own — not record content plus a constant. (b) Replay does not count as access: `replay_boot_sessions` never refreshes the cache's recency, and eviction decides "in the working set" by the session's own last event against `working_set_days`, never by when replay touched it. Sessions outside the window are evictable during replay the moment the budget says so; sessions inside it stay pinned as before; every session still rebuilds on first access. Added 2026-09-25 from the B-06 finding. | (a) `rs/store/tests/test_projection_heap.rs`: a projection built from a real-shaped 140-event session reports bytes within 20 % of a counting allocator's live measurement; the cache's byte-accounting specs stay green. (b) `rs/tests/test_replay_bounded.rs`: a store of 100 sessions, 10 inside the window, a budget that fits 20: after replay the cache holds the 10 plus at most the budget's worth, `projections.count` on health says so, and an evicted session rebuilds whole on first access. Gate: the 1259-session fixture reaches serving inside 2 g on Linux, anon well under the limit, `projections.count` bounded, the settle number holding. |
| B-10 | **The gate is a test.** `rs/tests/test_boot_memory_gate.rs`, opt-in like the other container tests (`just test-container`; needs Docker and `open-story:test` from `just docker-build`; skips with a message when Docker is not running). It builds a small synthetic store through the B-00 harness (`scripts/boot_memory.py --build-only`: 150 sessions, 8 large at 4000 events, 0.8 GB JSONL — sized so the pre-B-09 node, every projection resident, needs more than the limit and the bounded node does not), starts the production image under a 512 MiB cgroup with a `nats:2-alpine` JetStream sidecar on a private `bmgate-*` network, watches `docker inspect` and `/api/health` once a second until serving plus a 20 s settle, twice on one store (ingest, then the DB-present restart), and asserts: no OOM kill or restart; `projections.count` below the session count at serving and throughout replay; `process.memory_limit_bytes` equal to the cgroup limit; `process.allocator` = `jemalloc`; `process.rss_bytes` a number (the rule can only see pressure it can measure); no critical `memory_pressure` at the flip to serving and no `memory_pressure` finding at any level once settled. Added 2026-09-25 after B-09. | the test file, red at the fixture step (no `--build-only`), then red on `rss_bytes` null inside the image; pure specs in `test_memory_pressure.rs` (`rss_from_proc_status`) and `test_replay_bounded.rs` (`effective_payload_budget`); the numbers in the loop log |
| B-08 | **Roll only behind the gate.** Order: mini (compose, owner's box), hub (openstory-deploy workflow), a1. Each roll: B-00 number on the branch is green; the node reaches serving under its limit; `fleet_presence` shows it beating; one `node_verify` agrees. PR 4 (8G) closes unmerged. PR 5 (node compose cache lines) merges only if B-03 still wants the knobs. | loop log entries with `date` times and the health verdict at serving for each node |

## Order and size

1. **B-00** first, red: the number that fails today (the fixture will pass 2 GB on this branch — measured 2026-09-25: it did not on the owner's laptop, 1.70 GB, and the peak sits in replay, not the boot pass; see the loop log). Half a day.
2. **B-01 + B-02**: the morning fix. The hub's 3.4 GB pre-replay plateau goes away.
3. **B-05 + B-06 + B-07**: an afternoon. Deterministic boot, memory returned, and the verdict names pressure before the kernel does.
4. **B-03 + B-04**: the next day. The right shape: derived, on disk, rebuildable, one walk.
5. **B-08**: only when B-00 reads green on the branch.

## Tonight, before any of it

The owner rolls the mini back to `sha-f0a22d1` and either rolls the hub back the same way or lets it loop until B-08. Neither is a code change and neither is the agent's to run.

## What this found (2026-09-25, after B-07)

The "boot pass" this plan blames is a 200 MB phase on Linux; the plateau that kills the fleet is the boot *replay* holding every session's projection resident (≈ 2.1 MB per session; 2.7 GB for 1,259 sessions on the harness fixture, and the hub's 3,050 sessions would be ≈ 6 GB), because replay counts as access inside the working-set window and the cache's byte accounting undercounts. B-06's allocator change moves it by 5 %. The loop log's B-06 entry has the measurements and the two code sites. The next row is that fix; B-08 must not roll before it, whatever B-00 says on macOS.

## What this does not do

It does not change what is observed, persisted, or replayed. Every row reads less or reads later; nothing reads differently. `events.*` and `local.*` are untouched. The caches from July stay as they are.

## Loop log

Times are UTC from `date -u` / the commit stamps. Work in `~/projects/openstory-wt-kindle` on `feat/reel-chart-beats-kindle`; the live node on :3002 untouched; the fixture lives under the session scratchpad (`…/scratchpad/boot-memory/full`), not in git.

- **2026-09-25 14:24–14:27Z · B-00** — red `be18fc9` (11 specs, all `NotImplementedError`), green `1c88edf` (12 specs; `just boot-memory`). Fixture: 1259 sessions, 782,001 events, 3.07 GB JSONL, 40 large sessions at 8000 events, 126 subagents, seed 2026, built in 121 s. Adjustments against the row as written: tool outputs are sized to the byte budget (~5 KB per copy, three copies per tool_result, ~16 KB per event) rather than 80 KB — 40 × 8000 × 80 KB would be 6 GB alone; and the phases are the ones `/api/health` can distinguish from outside (no-listen covers reconcile + boot pass + working-set reproject together), since splitting reconcile from the boot pass would mean changing the node to report it. Binary: this branch's release build (code at `29ff751`). First boot (ingest, no DB): serving after 341 s, peak 895 MB, SQLite 4.79 GB. **Measured boot (DB present): serving after 92 s; no-listen 256 MB, replaying 1,704 MB, serving 354 MB, settle 36 MB; peak 1.704 GB < 2 GB → PASS.** So the gate is not red here, and the peak is in replay, not in the boot pass the plan blames. The hub's 3.4 GB pre-replay plateau does not reproduce on macOS with this fixture; the two candidates are glibc's allocator keeping freed pages (B-06's row) and real sessions being wider than the fixture (an owner-run pass on the mini's store copy would say). Side observation, pre-existing: the reconciler lists `events.jsonl` / `presence.jsonl` as sessions (the node reports 1260 sessions for 1259 files).
- **2026-09-25 14:30–14:36Z · B-01** — red `2a01ba3`, green `83a5776`. `EventStore::session_boot_facts` on SQLite (one `json_extract`/`COALESCE` query over `idx_events_session`, `LIMIT 32`) and Mongo (sort + limit 32 + projection to the five paths); the trait default reads the same window from `session_events` (JSONL and HTTP stores). One place for each rule: `analysis::CWD_FIELD_PATHS` (walked by `extract_cwd`, spelled for SQL by `json_path`), `state::parent_session_of` (used by `detect_subagent_relationship`). Conformance: 68/68 on SQLite; the three new helpers 3/3 on Mongo against a `mongo:7` testcontainer (Docker was already running; nothing was started), best-of-five under 5 ms on a 5000-event session. Clippy clean on the lib target; `--all-targets` on the store crate shows two pre-existing 1.96 lints (`projection.rs` items-after-test-module, a test helper in `queries.rs` with 8 arguments) left for the toolchain-drift cleanup already noted in REQUIREMENTS.
- **2026-09-25 14:41–14:46Z · B-02** — red `62a7c74` (recording `EventStore` in `rs/tests/helpers/recording_store.rs`; `create_state_with_store` seam; the spec saw `session_events` × 3), green `53198e7` (`boot_from_sqlite` calls `session_boot_facts` only; the parent is recorded through the shared `record_subagent_parent`), plus `71f058a` (harness: fixture reuse ignores the `events.jsonl` / `presence.jsonl` the node writes into its data dir — the second run refused the fixture until then). Existing subagent-parent and project-association specs (`test_api`, `test_ingest`, server crate 262) stay green. **B-00 after B-02, same fixture and DB, binary from `53198e7`: serving after 215 s; no-listen 200 MB (was 256), replaying 836 MB (was 1,704), serving 790 MB, settle 49 MB; peak 0.836 GB < 2 GB → PASS.** The boot-pass phase itself was small on this fixture both times (the mini is not where that pass hurt); the halving of the replay peak is the freed heap the old pass left behind for the replay to grow on top of. Time-to-serving is not stable run to run (92 s before, 215 s after; reconcile alone went 21 s → 43 s on the same DB), so read the RSS columns, not the seconds. What is left for the gate to mean something on the fleet: an owner-run pass on the mini's store copy, and B-06 for the allocator.
- **2026-09-25 14:50–14:58Z · gate** — `just test` from the worktree: every Rust test binary green except one, workspace clippy clean, the four static audits green, UI 2082/2082. The one failure is `test_health::when_leaf_is_configured_but_down::it_reports_not_connected`, which assumes nothing answers on `:8222`; on this laptop the live node's managed nats-server (off limits to the loop) listens there with one real leaf to the hub, so the test reads `connected: true`. Environmental, pre-existing, not touched. Next: B-05.
- **2026-09-25 15:04–15:20Z · B-05** — red `21121fe` (rs/tests/test_consumers_start.rs would not compile against the missing surface; two UI verdict specs failing), green `12b69b5`. `consumers_start = "serving"` (default) holds the five supervisors (persist, presence, patterns, projections, broadcast) on `boot::consumer_gate` until `set_serving()`; `"boot"` is today's shape; health carries `consumers.<name>.state` (pending_start / running / backoff / finished); the verdict — server, dashboard — never calls a held actor dead and warns `consumer_pending:<name>` only once the node serves. The probe script (`node_health_probe.py`) does not read the consumers block at all, so it needed nothing. Live on the fixture (`:3300`, binary from `12b69b5`): `503 replaying 0/1260 consumers={'pending_start': 5}` … `200 serving 1260/1260 consumers={'running': 5}`. **B-00 after B-05, macOS, same fixture and DB: serving after 231 s; no-listen 244 MB, replaying 498 MB (was 836 after B-02, 1,704 before), serving 397 MB, settle 49 MB; peak 0.498 GB.** The replay-phase peak is now the replay alone. Time-to-serving stayed noisy (231 s here). Clippy `--all-targets` on `rs/tests` binaries still hits the pre-existing `helpers/container.rs:212` `&PathBuf` lint (every test binary links the helpers); it is outside the `just test` gate and untouched.
- **2026-09-25 15:21–15:33Z · Linux mode for B-00** — red `8a1269e`, green in the next harness commit. `scripts/boot_memory.py --docker IMAGE --memory 2g` runs the measured boot in a container built from this worktree's `Dockerfile.prod` (`open-story:boot-memory`, built once; a `.dockerignore` was needed — the `ui/node_modules` symlink into the main checkout broke the build context), the fixture's `data/` bind-mounted at `/data`, an empty `/watch` for every watcher root, `--memory`/`--memory-swap` on the node only, and the bus on a `nats:2-alpine` JetStream sidecar on a private network (the prod image carries no `nats-server`, and the hub runs NATS as its own container, so a sidecar is the honest shape rather than `--manage-nats`). Samples about every 2 s: `docker stats` usage, cgroup v2 `memory.stat` anon/file, `/proc/1/status` VmRSS, the health phase, and B-05's consumer states. Docker never started or stopped; only the two `bm-*` containers were created and removed. **Linux (glibc), code at `12b69b5` (B-05, no jemalloc), 2 g limit: OOM-killed at t = 118 s, 65 % through replay; anon 460 → 2,123 MB linearly (≈ +30 MB/s) with the five consumers still `pending_start`; boot pass flat at ≈ 140 MB anon (file cache 2.0 GB from the SQLite reads, reclaimable); peak usage 2.136 GB → FAIL.** The same replay on macOS peaked at 498 MB. So the plateau reproduces on the hub's image and is the allocator's, not the consumers' or the boot pass's; B-06 is the row that must move it. (In this mode the pre-listen phase reads `no-answer`, not `no-listen`: the published port accepts TCP before the node binds.)
- **2026-09-25 15:36–15:50Z · B-07** — red `2538d9a` (three readers, three reds: the Rust spec would not compile, the probe raised NameError, one UI assertion failed), green `eb4b39f`. Health carries `process.memory_limit_bytes` (cgroup v2 `memory.max`; "max" and no cgroup read as null) next to `rss_bytes` and the B-06 `allocator`; `node_health::memory_pressure` is the one rule (warn at 75 %, critical at 90 %, text "memory at 92% of its 5.0 GB limit"), and the server verdict, `scripts/node_health_probe.py`, and the dashboard's `verdictFor` all apply it word for word. Inside the harness's 2 g container the field reads 2147483648, so the verdict on such a node goes warn at 1.6 GB and critical at 1.9 GB — before the kernel's kill at 2.14 GB.
- **2026-09-25 15:33–16:20Z · B-06** — red `f491e91`, green `aa0d702`. `open-story-cli` runs jemalloc by default (`tikv-jemallocator` 0.7 behind the default-on `jemalloc` feature; `--no-default-features` keeps glibc), with `_rjem_malloc_conf = background_thread:true,dirty_decay_ms:1000,muzzy_decay_ms:1000` read back through mallctl in the crate's own spec; health says `process.allocator`; the serve log's first lines say it too. `Dockerfile.prod` builds with defaults, so the image is the jemalloc binary (checked: `_rjem_malloc_conf` is in that image's binary and not in the glibc one). Numbers, Linux mode, same fixture, all on a store rebuilt once by the glibc image (the earlier 4 g pair was thrown away: I had let a macOS boot and a container boot share the SQLite file at the same time, the macOS binary died in `walFindFrame` with SIGBUS and the store read "disk image is malformed"; the harness never ran two boots on one store again):

  | image | limit | outcome | anon at replay peak | anon serving | anon settle (60 s) | usage peak |
  |---|---|---|---|---|---|---|
  | glibc, code `12b69b5` | 2 g | OOM-killed at 118 s, 65 % of replay | 2,123 MB | — | — | 2,136 MB |
  | glibc, code `12b69b5` | 4 g | serving after 115 s | 2,728 MB | 2,729 MB | 2,729 MB | 2,748 MB |
  | jemalloc, code `aa0d702` | 4 g | serving after 94 s | 2,643 MB | 2,586 MB | 2,583 MB | 2,565 MB |
  | jemalloc, code `aa0d702` | 2 g | OOM-killed at 83 s | 2,083 MB | — | — | 2,143 MB |

  The settle number fell (2,729 → 2,583 MB, −5 %) and the peak with it (−3 %), so the row reads green by its own letter — and it does not move the plateau. The macOS numbers (498–539 MB) are not this number: macOS compresses idle heap out of the resident set (the settle sample there fell to 36–49 MB thirty seconds after serving), so from here the Linux mode is the measurement and the macOS run is a smoke test. **What the plateau is:** the health monitor during both 4 g boots showed `projections.count` tracking `boot.replay.done` one for one (253 resident at 252 replayed, 1,011 at 1,008, 1,260 at serving), and anon stays at that level through serving and settle: it is the store's whole read model, ≈ 2.1 MB per session, held live. Two causes in `rs/store/src/projection_cache.rs` / `projection.rs`, neither an allocator: `evict_to_budget` never evicts a session accessed within `working_set_days` (7) by `Instant::now()` recency, and `replay_boot_sessions` refreshes that recency for every session it walks, so during replay nothing is evictable whatever the budget; and `SessionProjection::heap_bytes` counts record content plus 256 B per record and leaves out `seen_ids`, `depths`, `parents`, `filter_counts`, `file_touches`, and the `ViewRecord` structs, so the 4 GB default budget (`projection_cache_bytes`) is reached far later in real bytes than in accounted ones. That is the "not inside the bounded caches" reading from the hub's gauges: the gauge reports the undercount. The row that must come next is not in this plan: replay must not count as access (evict by the session's own `last_event` against the window, or walk with a bounded resident set), and `heap_bytes` must count what it holds; B-03/B-04 as written would leave this plateau in place. Nothing of that was started — it is outside B-05..B-07.
- **2026-09-25 16:24–17:02Z · B-09** — red `1184fff` (a: `heap_bytes` 258,528 B against 378,267 B measured by a counting allocator, ratio 0.68; b: 100 of 100 resident after replay against a budget of 20 + 10 in the window), green `3b1e64b`; then a third part the first Linux run forced, red `203a5c8` (in `test_replay_bounded.rs`), green `b641439`: **(c) the budget is clamped to the box.** With (a)+(b) alone and the default `projection_cache_bytes` of 4 GB, both images were still OOM-killed at 2 g — now that `heap_bytes` counts real bytes, the cache honestly fills toward 4 GB inside a 2 GiB cgroup. `effective_projection_budget(configured, memory_limit)` takes the smaller of the configured value and 40 % of cgroup v2 `memory.max` (logged as `event=projection_budget_clamped`); no limit known, the configured value stands; an operator's smaller setting is kept. That is an adjustment to the row as the coordinator wrote it (two changes); it is the smallest one that makes the bound real on a box, and it is stated here.

  What changed under (a): `SessionProjection` keeps `owned_bytes` as a running sum on append (every record's strings and values, full payloads, map keys) and `heap_bytes` adds the containers at capacity — O(1) now, where the old walk was O(records) per event; and it records `last_event_at` (latest non-synthesized event time). Under (b): the cache's recency instant is `Option` — `None` for an entry only a replay walked; `append_untouched` / `peek` are the replay's verbs; eviction protects a session read or live-appended within the window or whose own `last_event_at` is inside it, never one only a replay touched; replay pins the session it is walking so a half-built projection is never the victim; `projections.fresh` now means "replay complete" (with a bounded cache, count < sessions is the design). Specs: `test_projection_heap.rs` (20 % of the allocator), `test_replay_bounded.rs` 3 modules, the cache's own specs rescaled to a measured unit plus one for the replay walk.

  **Linux, 2 g, same fixture and store, sequential (never two boots on one store):**

  | image | code | outcome | anon replay peak | anon serving | anon settle (60 s) | resident projections | cache gauge |
  |---|---|---|---|---|---|---|---|
  | glibc | `12b69b5` (B-05) | OOM-killed at 118 s | 2,123 MB | — | — | 1:1 with replay | — |
  | jemalloc | `aa0d702` (B-06) | OOM-killed at 83 s | 2,083 MB | — | — | 1:1 with replay | — |
  | glibc | `3b1e64b` (B-09 a+b, 4 GB budget) | OOM-killed at 175 s | 2,087 MB | — | — | 768 at 756 | — |
  | jemalloc | `3b1e64b` (B-09 a+b, 4 GB budget) | OOM-killed | ≈ 2,100 MB | — | — | 796 at 756 | — |
  | glibc | `b641439` (B-09 a+b+c) | **serving after 89 s** | 1,511 MB | 1,516 MB | 1,516 MB | 524 of 1,260 | 842 MB of an 859 MB clamp, 274+ evictions |
  | jemalloc | `b641439` (B-09 a+b+c) | **serving after 87 s** | 1,472 MB | 1,463 MB | 1,397 MB | 524 of 1,260 | same clamp |

  The gate reads green on both images: serving inside 2 g, anon at 70 % of the limit (below B-07's 75 % warn — the verdict at serving was `ok`), `projections.count` bounded at 524 and holding through replay's second half, the settle number holding (glibc) or falling 66 MB (jemalloc). During replay `projections.count` held at 492–494 from bucket 3 on, where before it tracked replay 1:1. What the remaining ~650 MB above the cache gauge is: the payload cache at its 256 MB cap, the reconciler's per-session read of 3 GB of JSONL (bind-mounted; it accounts for the first 35 s), replay temporaries, and allocator slack — none of it grows with the store, which is what the fleet needed. A hub at a 5 GB limit gets a 2 GB clamp and ≈ 2.7 GB total by the same arithmetic; that is an owner-run measurement, not a promise.
- **2026-09-25 19:30–20:53Z · B-10** — red `6ad37e8` (the spec fails at the fixture step: the harness had no `--build-only`), then three more reds the gate itself forced — `c3bed57` (`rss_bytes` is null inside the image and the payload cache is not clamped), `ae5840c` (the projection share is 30 %), `554df69` (pressure is judged at the flip and once settled) — and green in the next two commits (code, then this log). `just test-container` now runs `test_container` and the gate; `docs/deploy/operations.md` points at it. Fixture: 150 sessions, 155,114 events, 0.82 GB JSONL, 8 large at 4000 events (≈ 61 MB each), 15 subagents, seed 2026, built by `scripts/boot_memory.py --build-only` in 45–49 s (254 s once, while the image was building beside it); the test builds it in a temp dir (`BMGATE_FIXTURE_DIR` reuses one), the loop-log runs used a copy under the scratchpad. Containers: the test's `bmgate-*` node, sidecar, and network, removed on drop; the harness's `bm-*` pair for the two harness runs, removed by the harness; never two boots on one store. What the gate found, in order, all on the same fixture inside `--memory 512m` with a `nats:2-alpine` sidecar:

  | run | image / code | outcome |
  |---|---|---|
  | R2 | `open-story:boot-memory` — `12b69b5` (B-05, glibc, every projection resident), DB present | **OOM-killed at t ≈ 12 s**, exit 137, two samples into replay: anon 420 MB and climbing (pre-listen anon 105 MB). Everything resident for this store is ≈ 0.88 × 0.82 GB ≈ 720 MB plus the floor — the fixture is sized right. |
  | R1 | `open-story:test` at `86882d9` (B-09 a+b+c, jemalloc), two boots | survives, at the wall: ingest boot serving after 444 s, anon peak 532.7 MB, usage 536.8 MB = 100 % of the limit (the page cache reclaimed continuously; the DB-present boot took 117 s); DB-present boot anon 525 MB peak, 505 MB serving, 495 MB settle. Verdict `ok` — because `process.rss_bytes` was **null**: B-07's reader shells out to `ps`, which debian-slim does not ship. The arithmetic: 40 % projection clamp (215 MB) + the unclamped 256 MB payload cache, which replay fills (`projection.rs:471`), + the floor. |
  | probe | payload cache clamped to 10 % (54 MB), share still 40 %, `/metrics` on, DB present | serving after 17 s (the thrash gone); rss 390 MB = 72.6 %; RssAnon 365 MB, RssFile 15 MB; projection cache 174 MB (37 resident), payload cache 54 MB; cgroup anon 375 MB, file 65 MB. The **non-cache floor is ≈ 137 MB, 27 % of a 512 MiB box** (binary, tokio, NATS client, SQLite, FTS, log ring, allocator slack). |
  | candidate 1 | rss from `/proc`, payload clamp, share 40 % | ingest boot serves at 428 MB → `memory_pressure` warn at 80 % → red. Two caches at 50 % of the box plus a 27 % floor cannot sit under the 75 % warn. |
  | candidate 2 | share 30 % | ingest boot serves at 384 MB (71.5 %, ok); DB-present boot's first 200 carries 428.7 MB (80 %, warn) and reads 370.9 MB two seconds later, 371.5 MB at seventeen — a transient at the flip, where B-05 starts the five consumers and the last replay temporaries are still being returned. Spec adjusted: at the flip nothing critical; settled (20 s), no finding. |
  | **green** | `open-story:test` from this branch, `just test-container` | **boot 1 (ingest): serving after 286 s, peak rss 415 MB, 374 MB serving (70 %), 27 of 150 resident, max 28 during replay, verdict ok → ok. boot 2 (DB present): serving after 21 s, peak rss 406 MB, 351 MB serving (65 %), 28 of 151 resident, max 29, verdict ok → ok.** `projections.count` held at 21–29 from the first replay bucket on. |

  The `just test-container` result: `test_boot_memory_gate` 1 passed in 398.19 s; `test_container` 15 passed in 9.00 s; recipe 546 s including the cached image build and the test compile, run beside `just test`. The gate itself is ≈ 6.5 min: 45 s fixture, ≈ 4.8 min for the ingest boot (reconcile of 0.82 GB into a fresh WAL under a 512 MiB cgroup: 75 s to listen, then 150 sessions replayed in 180 s where the DB-present boot replays them in 17 s — the page cache thrashing on the fresh store, a thread, not a fault), 21 s for the DB-present boot, two 20 s settles.

  Adjustments to the row as the coordinator wrote it, each stated: (1) `process_rss_bytes` reads `/proc/self/status` first and falls back to `ps` — without it the verdict is blind on every containerized node and the "no finding" assertion is vacuous; (2) `effective_payload_budget`, 10 % of the cgroup limit, beside the projection clamp (the two caches take at most 40 % of the box; a 5 GiB box keeps the 256 MB default); (3) `PROJECTION_BUDGET_SHARE_PCT` 40 → 30 — the hub's clamp at 5 GB becomes 1.5 GB, and B-09 measured 524 sessions in 859 MB, so that still holds ≈ 900; (4) pressure is judged at the flip (not critical) and settled (none), not at the single instant of the first 200 — the +55 MB transient at the flip is a thread to pull (B-05's consumer start, or jemalloc's 1 s decay of the last session's temporaries); (5) two sequential boots, not one, so the ingest path and the fleet's DB-present restart are both gated. `just test` from the worktree: see the next entry.
- **2026-09-25 20:55–21:20Z · gate after B-10** — `just test` from the worktree, the Rust half rerun with `--no-fail-fast` (the recipe stops at the first failing binary): 147 test binaries green, clippy clean at `-D warnings`, the three static audits and `check_docs` green (17/17), UI 2083/2085. Not green, none of it B-10's: `test_health::when_leaf_is_configured_but_down::it_reports_not_connected` (the live node's managed nats-server on `:8222`, the same environmental failure the B-02 entry recorded); `test_pi_mono_container::pi_mono_session_metadata_persisted` read `event_count: 0` once under the load of the whole workspace run and passes 2/2 rerun alone in about a second — the sessions list shows before the persist consumer, which since B-05 starts at serving, writes the row: an eventual-consistency read in a test that does not poll, pre-existing (its name escapes `--skip container`, so `just test` runs it); the two UI misses are the timing benchmarks `reducer-bench` (< 250 ms) and `timeline-bench` (< 15 ms) beside forty Rust test binaries and Docker, 22/22 rerun unloaded. Next: B-03/B-04, or the two threads this row left — the +55 MB transient at the flip to serving and the ingest boot's replay under a small cgroup (180 s against 17 s for the DB-present boot).
