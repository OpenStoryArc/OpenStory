# Grok Build golden fixtures — provenance

Two layers of seed data:

## 1. Synthetic scenarios (deterministic unit/golden)

Generated by `scripts/gen_grok_fixtures.py`. Wire-faithful ACP shapes.
Fixed session id: `019f6cb5-f7e4-7bc1-bb25-aaaaaaaaaaaa`

| File | Intent |
|------|--------|
| `scenario_01_text_only.jsonl` | prompt → thought → text → turn_completed |
| `scenario_02_single_tool.jsonl` | list_dir (+ in_progress skipped) |
| `scenario_03_multi_tool.jsonl` | read_file + run_terminal_command |
| `scenario_04_error_recovery.jsonl` | failed read → recovery |
| `scenario_05_edit_and_test.jsonl` | search_replace + cargo test |

```
python3 scripts/gen_grok_fixtures.py
```

## 2. Real session seed (this OpenStory integration conversation)

Extracted from the live Grok Build session that *implemented* Grok support:

```
~/.grok/sessions/%2FUsers%2Fmaxglassie%2Fprojects%2Fgrok-build/
  019f6cb5-f7e4-7bc1-bb25-9985af59619e/updates.jsonl
```

| File | Source turn | Prompt |
|------|-------------|--------|
| `real_turn_01_text_only.jsonl` | 1 | what is TUI? |
| `real_turn_02_session_storage.jsonl` | 2 | where does grok build keep session data? |
| `real_turn_07_openstory_vs_grok.jsonl` | 7 | How does OpenStory relate to Grok Build? |
| `real_turn_09_acp_and_mcp.jsonl` | 9 | what is acp? / openstory mcp |
| `seed_tree/…/updates.jsonl` | 1+2+7+9 | nested layout for watcher / **container** mounts |
| `SESSION_SEED.md` | — | full extract notes |

```
python3 scripts/extract_grok_session_seed.py
```

Tool outputs truncated for golden size; use `--no-truncate` for full wire.

## Container e2e (eventually)

`rs/tests/test_grok_container.rs` — layout always-on; Docker path `#[ignore]`
until `open-story:test` image is built. Mount `fixtures/grok/seed_tree` as
watch root (same shape as `~/.grok/sessions`).
